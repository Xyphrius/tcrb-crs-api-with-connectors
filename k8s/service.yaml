apiVersion: v1
kind: Service
metadata:
  name: tcrb-crs-api
  labels:
    app: tcrb-crs-api
spec:
  type: ClusterIP
  selector:
    app: tcrb-crs-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
